export enum BtnType {
    button = 'button',
    submit = 'submit',
}

export enum BtnVariant {
    secondary = 'secondary',
    primary = 'primary',
}

export enum Size {
    small = 'small',
    big = 'big',
}

